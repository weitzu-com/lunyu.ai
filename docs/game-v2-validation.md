# 游戏人物、账号与交互验收 · 2026-09-06

## 已完成

- 孔子及77位弟子全部逐人参照原画转换，共78幅；完整来源和SHA-256见 `docs/research/game-character-artwork.json`。
- 78幅WebP均可解码、640×640、像素哈希互不相同，总计约1.52MiB。浏览器逐幅滚动加载78/78成功，URL无重复。
- 人物馆的别称搜索、无结果恢复、分类筛选、首尾翻页、原画对照、键盘焦点循环、手机底部关闭均通过。
- 游戏完整走过八章16幕、记录八段手记、总分48、导出Markdown、刷新恢复；已完成选项不可重复增加分数。
- 320、375、414、768、1024、1440px宽度的首页和人物馆均无水平溢出；375px完成整场游戏。
- 动画开关保存到本地；系统减少动态效果设置生效。服务器首屏默认暂停动画，客户端读取用户偏好后再开启，避免已关闭用户刷新时闪现动画。
- 真实后端测试验证已有账号登录、退出、跨账号RLS隔离、CAS并发冲突、再次登录恢复、输入校验和同源请求校验。所有一次性测试账号已清理。
- 真实浏览器验证HttpOnly/SameSite/Secure cookie；页面JavaScript不能读取会话token。断网退出提供导出与明确放弃选项，完全离线时不会错误宣称已退出。
- 修复并回归验证保存中继续编辑、退出前flush、跨标签切账号、登录失效后恢复账号草稿、刷新时会话失效、空白草稿与游客手记分别保留。

## 检查命令

`npm run qa:game`、`npm run lint`、`npm run build`、`npm run qa:seo`、`npm run qa:biographies -- --built`、`npm run qa:coverage` 均通过。浏览器实际验收产物保存在本地 `output/playwright/v2-*`。

## 发布前仍需完成

新建Supabase项目 `lunyu-game` 的 Confirm email 仍开启；未配置公众SMTP。本版邮箱仅作为登录标识，计划关闭Confirm email后直接注册，不声称邮箱所有权已验证、不提供邮件找回。

自动化浏览器管理页持续连接超时，已请项目所有者在 Authentication → Sign In / Providers 关闭 Confirm email 并保存。应用读取真实 `/auth/v1/settings`，当前明确关闭注册入口，注册API返回503，不会给用户虚假的注册成功提示。该设置生效后5秒内自动开放，不需重新发版。

完成设置后必须再执行真实公众注册测试，并确认创建、登录、存档、退出和跨设备恢复均成功，再合并上线。生产网站当前保留此前已上线版本。
